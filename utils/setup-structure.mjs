// setup-structure.mjs
// GENERATED BY CHATGPT
// command to run the script : node setup-structure.mjs

import fs from "fs";
import path from "path";

const structure = [
    "db/connection.js",
    "models/client.model.js",
    "models/chien.model.js",
    "models/service.model.js",
    "models/mandat.model.js",
    "models/localite.model.js",
    "controllers/client.controller.js",
    "controllers/chien.controller.js",
    "controllers/service.controller.js",
    "controllers/mandat.controller.js",
    "controllers/localite.controller.js",
    "routes/client.routes.js",
    "routes/chien.routes.js",
    "routes/service.routes.js",
    "routes/mandat.routes.js",
    "routes/localite.routes.js",
    "utils/helper.mjs",
    "utils/utils.js",
    "app.js",
    ".env",
    ".gitignore",
    "README.md"
];

for (const filePath of structure) {
    const dir = path.dirname(filePath);
    if (dir !== "." && !fs.existsSync(dir)) {
        fs.mkdirSync(dir, { recursive: true });
        console.log("📁 Dossier créé :", dir);
    }
    if (!fs.existsSync(filePath)) {
        fs.writeFileSync(filePath, "");
        console.log("📄 Fichier créé :", filePath);
    }
}

console.log("\n✅ Structure MVC créée avec succès !");
